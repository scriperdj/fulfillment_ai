[pytest]
asyncio_mode = strict
addopts = -m "not integration"
markers =
    integration: requires Docker Compose services running (deselected by default)
    airflow: requires Airflow services running (subset of integration)
